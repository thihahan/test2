import * as bootstrap from "bootstrap";
import VenoBox from "venobox";

new VenoBox({
    selector : ".venobox",
    numeration: true,
    infinigall: true,
    share: true,
    spinner: 'rotating-plane'
})
